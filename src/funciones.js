const obtenerTitulo = ()=>'Bootcamp desarrollo fullstack con Node.js';

const obtenerEjecutor = (autor)=> `El ejecutor del bootcamp es ${autor}`;

const obtenerMentor = (mentor) => `El mentor del bootcamp es ${mentor}`;

const obtenerMonitor = (monitor) => `El monitor del bootcamp es ${monitor}`;

module.exports = {
    obtenerTitulo,
    obtenerEjecutor,
    obtenerMentor,
    obtenerMonitor
}

